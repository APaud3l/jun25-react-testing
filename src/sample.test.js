describe('Sanity check', () => { 
    it('adds numbers correctly', () => {
        // Arrange + Act
        const result = 1 + 1;

        // Assert
        expect(result).toBe(2);
    });
 });